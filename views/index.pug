extends layout

block content
  div(class='text-center content-box')
    h1= header
    if error
      p Error getting dynamic content.
    else
      p The inventory has the following record counts:

        p #[strong Categories:] !{data.category_count}
        p #[strong Items:] !{data.item_count}
  
  
